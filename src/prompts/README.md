# Prompts Boundary

Fronteira futura para separar identidade, comportamento, web, VM, Runner e seguranca do system instruction da Alice.

A extracao deve manter o texto final gerado por `createAliceLiveSetup` equivalente, com teste antes de alterar conteudo ou politica.
